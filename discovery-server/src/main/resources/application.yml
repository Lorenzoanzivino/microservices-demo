spring:
  application:
    name: discovery-server
  profiles:
    active: dev
  config:
    # SECURITY NOTE: Using HTTP for local development on localhost.
    # Production environments MUST use HTTPS with TLS termination.
    import: "optional:configserver:http://localhost:8071"

server:
  port: 8761